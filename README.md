

![](https://github.com/zyivan356/vizual_python/blob/master/assets/main2.png)

# Engineering Suite

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**Engineering Suite** — это модульный программный комплекс для инженерного проектирования и расчётов, сочетающий геометрическое моделирование, МКЭ-анализ и BIM-функционал. Проект создан для замены коммерческих решений (Plaxis, Renga) с открытым исходным кодом и гибкой архитектурой.

## 🔥 Ключевые возможности

- **Геометрическое ядро** на базе FreeCAD для создания 3D-моделей
- **МКЭ-расчёты** напряжений и деформаций с использованием Cython-ускорения
- **BIM-импорт/экспорт** в формате IFC через ifcopenshell
- **Генерация КЭ-сеток** с помощью Gmsh
- **Интерактивная 3D-визуализация** результатов (PyVista + Qt)
- **Плагинная архитектура** для расширения функционала (геотехника, строительные конструкции)
- **Интерактивное проектирование** фундаментов и вспомогательных конструкций

## 🖥️ Требования к системе

| Компонент | Версия | Примечание |
|---|---|---|
| ОС | Windows 10/11 | Рекомендуется WSL2 для Linux-сборок |
| Python | 3.11.x | **Не работает на Python 3.12+** из-за ограничений пакетов |
| Дополнительно | VC++ Redistributable | Обязательно для Windows! |
| Дополнительно | Build Tools для Visual Studio | Компонент "Desktop development with C++" |

## 🚀 Установка и запуск

### Для Windows:
```powershell
git clone https://github.com/zyivan356/vizual_python.git
cd vizual_python

# Создание виртуального окружения
python -m venv .venv
.venv\Scripts\activate

# КРИТИЧЕСКИ ВАЖНО: фиксация версий для Windows
pip install -r requirements.txt --only-binary=:all:

# Компиляция Cython-модулей
python setup.py build_ext --inplace

# Запуск приложения
python app.py
```

### Для Linux (рекомендуется WSL2):
```bash
# В Ubuntu 22.04 (WSL2)
sudo apt update && sudo apt install python3-pip python3-dev libgl1 libsm6 -y
git clone https://github.com/zyivan356/vizual_python.git
cd vizual_python
pip3 install -r requirements-linux.txt
python3 app.py
```

## 🗂️ Структура проекта

```
vizual_python/
├── core/                  # Нативные модули (Cython/Numba)
│   ├── stress_calculator.pyx  # Алгоритмы расчёта напряжений
│   └── __init__.py
├── plugins/               # Расширяемый функционал
│   ├── geotech/           # Геотехнические расчёты (аналог Plaxis)
│   └── structural/        # Строительные конструкции (аналог Renga)
├── bim/                   # Работа с IFC-файлами
├── viz/                   # 3D-визуализация (PyVista)
├── gui/                   # Интерфейс (PyQt5)
├── app.py                 # Точка входа
└── requirements.txt       # Фиксированные версии зависимостей
```

## 💻 Пример использования

```python
from plugins.structural import Beam
from viz.scene_manager import QtPyVistaWidget

# Создание модели
beam = Beam(length=5.0, width=0.3, height=0.5)
beam.apply_load(10e3)  # 10 кН

# Расчёт напряжений
stress_results = beam.calculate_stress()

# Визуализация
scene = QtPyVistaWidget()
scene.show_model(beam.geometry)
scene.show_stress_map(stress_results)
scene.start()
```

## ⚠️ Важные примечания

1. **Только Python 3.11.x** — более новые версии не поддерживаются из-за отсутствия бинарных сборок для инженерных пакетов
2. **Обязательная установка**:
   - Visual C++ Redistributable
   - Build Tools для Visual Studio (компонент "Desktop development with C++")
3. **Если возникают ошибки DLL**:
   ```powershell
   pip install PyQt5==5.15.9 PyQt5-sip==12.13.0 --only-binary=:all: --force-reinstall
   ```

## 🤝 Как внести вклад

1. Форкните репозиторий
2. Создайте ветку для вашей фичи (`git checkout -b feature/your-feature`)
3. Закоммитьте изменения (`git commit -am 'Add some feature'`)
4. Запушьте ветку (`git push origin feature/your-feature`)
5. Создайте Pull Request

**Приоритетные задачи для контрибьюторов:** 
- Интеграция с Code_Aster для промышленных расчётов
- Поддержка Linux/macOS без WSL2
- Экспорт результатов в PDF-отчёты
- Улучшение интерактивного проектирования фундаментов

## 📜 Лицензия

Этот проект распространяется под лицензией **MIT** — см. файл [LICENSE](LICENSE) для подробностей.

## 🙏 Благодарности

Проект использует следующие открытые библиотеки:
- FreeCAD — геометрическое ядро
- Code_Aster — промышленный МКЭ-решатель
- IfcOpenShell — BIM-функционал
- PyVista — 3D-визуализация

---

**ВАЖНО:** Этот проект находится на стадии MVP. Для промышленного использования требуется валидация расчётов и интеграция с сертифицированными решателями (Code_Aster, CalculiX). **Не используйте для реальных инженерных объектов без подтверждения результатов в коммерческих системах!**
